name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Set up Python
        run: uv python install

      - name: Install project
        run: uv sync

      - name: Check imports
        run: uv run python -c "import conduto.cli, conduto.env.env_render, conduto.schemas.schemas_render, conduto.project_base.gerar_project"

      - name: Smoke test CLI
        run: uv run conduto --help

      - name: Build package
        run: uv build
